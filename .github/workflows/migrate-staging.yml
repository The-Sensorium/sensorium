name: Migrate Staging

# Applies pending migrations to the STAGING Supabase project whenever code
# lands on `develop`. Feature branches never apply migrations directly; their
# migration SQL is applied here, on the develop merge, after PR review.

on:
  push:
    branches: [develop]
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
  workflow_dispatch:

concurrency:
  group: migrate-staging
  cancel-in-progress: false

jobs:
  migrate:
    name: Apply pending migrations to staging
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # No `supabase link` here: link fetches the project's legacy api-keys and
      # the platform 403s that call even for owners (supabase/cli#6392), so the
      # db steps below connect directly via --db-url instead of --linked.
      - name: Build staging database URL
        run: |-
          ENCODED_PASSWORD=$(python3 -c 'import os,urllib.parse; print(urllib.parse.quote(os.environ["SUPABASE_STAGING_DB_PASSWORD"], safe=""))')
          echo "STAGING_DB_URL=postgresql://postgres:${ENCODED_PASSWORD}@db.${SUPABASE_STAGING_PROJECT_ID}.supabase.co:5432/postgres" >> "$GITHUB_ENV"
        env:
          SUPABASE_STAGING_DB_PASSWORD: ${{ secrets.SUPABASE_STAGING_DB_PASSWORD }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}

      - name: Apply migrations to staging
        run: npx supabase db push --db-url "$STAGING_DB_URL"

      - name: Deploy send-emails edge function to staging
        run: npx supabase functions deploy send-emails --project-ref "$SUPABASE_STAGING_PROJECT_ID" --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}

      - name: Deploy send-push edge function to staging
        run: npx supabase functions deploy send-push --project-ref "$SUPABASE_STAGING_PROJECT_ID" --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}

      - name: Set push secrets on staging edge function
        run: >-
          npx supabase secrets set
          --project-ref "$SUPABASE_STAGING_PROJECT_ID"
          SENSORIUM_PUSH_SECRET="$SENSORIUM_PUSH_SECRET"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}
          SENSORIUM_PUSH_SECRET: ${{ secrets.SENSORIUM_PUSH_SECRET }}

      - name: Point the DB push cron at the staging edge function
        run: >-
          npx supabase db query --db-url "$STAGING_DB_URL"
          "update public.push_settings
             set edge_url = 'https://$SUPABASE_STAGING_PROJECT_ID.functions.supabase.co/send-push',
                 secret = '$SENSORIUM_PUSH_SECRET',
                 enabled = true,
                 updated_at = now();"
        env:
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}
          SENSORIUM_PUSH_SECRET: ${{ secrets.SENSORIUM_PUSH_SECRET }}

      - name: Set email secrets on staging edge function
        run: >-
          npx supabase secrets set
          --project-ref "$SUPABASE_STAGING_PROJECT_ID"
          RESEND_API_KEY="$RESEND_API_KEY"
          RESEND_FROM="no-reply@thesensorium.online"
          SENSORIUM_EMAIL_SECRET="$SENSORIUM_EMAIL_SECRET"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          SENSORIUM_EMAIL_SECRET: ${{ secrets.SENSORIUM_EMAIL_SECRET }}

      - name: Point the DB cron at the staging edge function
        run: >-
          npx supabase db query --db-url "$STAGING_DB_URL"
          "update public.email_settings
             set edge_url = 'https://$SUPABASE_STAGING_PROJECT_ID.functions.supabase.co/send-emails',
                 secret = '$SENSORIUM_EMAIL_SECRET',
                 app_url = 'https://preview.thesensorium.online',
                 enabled = true,
                 updated_at = now();"
        env:
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}
          SENSORIUM_EMAIL_SECRET: ${{ secrets.SENSORIUM_EMAIL_SECRET }}

      # create-call-token is user-invoked (not cron), so JWT verification stays
      # on (config.toml verify_jwt = true) — no --no-verify-jwt here.
      - name: Deploy create-call-token edge function to staging
        run: npx supabase functions deploy create-call-token --project-ref "$SUPABASE_STAGING_PROJECT_ID"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}

      - name: Set LiveKit secrets on staging edge function
        run: >-
          npx supabase secrets set
          --project-ref "$SUPABASE_STAGING_PROJECT_ID"
          LIVEKIT_URL="$LIVEKIT_URL"
          LIVEKIT_API_KEY="$LIVEKIT_API_KEY"
          LIVEKIT_API_SECRET="$LIVEKIT_API_SECRET"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_STAGING_ACCESS_TOKEN }}
          SUPABASE_STAGING_PROJECT_ID: ${{ secrets.SUPABASE_STAGING_PROJECT_ID }}
          LIVEKIT_URL: ${{ secrets.LIVEKIT_URL }}
          LIVEKIT_API_KEY: ${{ secrets.LIVEKIT_API_KEY }}
          LIVEKIT_API_SECRET: ${{ secrets.LIVEKIT_API_SECRET }}
